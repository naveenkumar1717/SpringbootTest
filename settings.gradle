pluginManagement {
  repositories {
    maven { url "https://plugins.gradle.org/m2/" }
    gradlePluginPortal()
  }
  plugins {
     id "org.sonarqube" version "4.0.0.2929"
  }
}

rootProject.name = 'springboot'
