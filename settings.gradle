pluginManagement {
  repositories {
    maven { url "https://plugins.gradle.org/m2/" }
    gradlePluginPortal()
  }
  plugins {
    id "org.sonarqube" version "10.4.1"
  }
}

rootProject.name = 'springboot'
